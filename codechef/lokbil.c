#include<stdio.h>
typedef struct table{
	int *array[100];
}table;

int main(){
	table a;
	int g, f, ch;
	scanf("%d", &g);
	while(g--){
		scanf("%d", &f);
		for(i = 1; i <= f; i++){
			while((c = getchar()) != '\n'){
				if( c >= '0' && c <= '9')
			
						

			}

		}

	}
	return 0;
}
